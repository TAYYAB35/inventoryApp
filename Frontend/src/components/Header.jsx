import React from 'react'
import { Layout } from 'antd';

const Header = () => {
    const { Header } = Layout;

    return (
        <div>
            <Header  >

            </Header>
        </div>
    )
}

export default Header